@page
@model PensiuneaLotus.Pages.Reservations.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="Reservation.RoomID" class="control-label"></label>
                <select asp-for="Reservation.RoomID" class ="form-control" asp-items="ViewBag.RoomID" aria-required="true"></select>
            </div>
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="Reservation.GuestID" class="control-label"></label>
                <select asp-for="Reservation.GuestID" class ="form-control" asp-items="ViewBag.GuestID" aria-required="true"></select>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.CheckInDate" class="control-label"></label>
                <input asp-for="Reservation.CheckInDate" class="form-control" />
                <span asp-validation-for="Reservation.CheckInDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.CheckOutDate" class="control-label"></label>
                <input asp-for="Reservation.CheckOutDate" class="form-control" />
                <span asp-validation-for="Reservation.CheckOutDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
